name: sistema_parqueadero_sprint3
description: Proyecto de Parqueadero con integración de notificaciones push vía WebSockets.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  socket_io_client: ^2.0.1
  provider: ^6.0.5
  cupertino_icons: ^1.0.2
  sqflite: ^2.3.0
  path: ^1.8.3
  firebase_core: ^2.24.2
  cloud_firestore: ^4.13.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
